/**
 * Layout constants for GrammarMapClient.
 * Centralized to document intent and avoid magic numbers.
 */

/** Width of each grammar topic node in the map (px) */
export const NODE_WIDTH = 220;

/** Height of each grammar topic node in the map (px) */
export const NODE_HEIGHT = 90;
