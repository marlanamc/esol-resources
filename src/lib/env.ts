// Re-export from new location for backward compatibility
export {
  buildEnvVarConfig,
  validateEnv,
  getRequiredEnv,
  getOptionalEnv,
  getAppBaseUrl,
  isProduction,
  isDevelopment,
} from "./shared/env";
